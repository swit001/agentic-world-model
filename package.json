{
  "name": "agentic-world-model",
  "version": "0.1.0",
  "private": true,
  "description": "Define executable business worlds for production AI agents.",
  "scripts": {
    "build": "pnpm --filter @agentic-world-model/core build && pnpm --filter @agentic-world-model/cli build",
    "test": "pnpm -r test",
    "awm": "node packages/cli/dist/index.js"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
